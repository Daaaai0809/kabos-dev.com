import { Meta } from "@storybook/react";
import { SideMenu } from "..";

const meta = {
    title: "Components/SideMenu",
} satisfies Meta;

export default meta;

export const Overview = () => <SideMenu />;
